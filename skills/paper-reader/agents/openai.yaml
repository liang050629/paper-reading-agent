interface:
  display_name: "Paper Reader"
  short_description: "Orchestrate evidence-grounded paper reading"
  default_prompt: "Use $paper-reader to analyze this paper with source evidence and generate the optional Poster branch only when requested."
